 <!-- What is the difference between var, let, and const? -->

 var -- Function scoped , not block scoped. Can be redeclared and reassigned.
 let -- not static can be  reassigned also block scoped
 const -- Block scoped but static. Can not reassigned the value once it is assigned.

  <!-- What is the spread operator (...)? -->
  It is used to expand element of array, object or function called.'

  <!-- What is the difference between map(), filter(), and forEach()? -->
  map() == transform elements into array and create a new array

  filter() == select an element from an array and check if it meets the condition.

  forEach() == to execute  the provided function for every iteration

  <!-- What is an arrow function? -->
  It's a shorter form of function. You  can skip function keyword, curly braces , return keyword.

  <!-- What are template literals? -->
  Backticks (" ` ` ")